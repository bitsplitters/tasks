{
  "name": "tasks-docs",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "rehype-external-links": "^3.0.0"
  }
}
